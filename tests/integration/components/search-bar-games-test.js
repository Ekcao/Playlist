import { moduleForComponent, test } from 'ember-qunit';
import hbs from 'htmlbars-inline-precompile';

moduleForComponent('search-bar-games', 'Integration | Component | search bar games', {
  integration: true
});
