{{#paper-autocomplete
    class="search-bar-games"
    noMatchesMessage="No matches found."
    loadingMessage="Searching games..."
    placeholder="Search games..."
    searchField="name"
    labelPath="name"
    allowClear=true
    searchText=searchGameText
    selected=selectedGame
    onSearchTextChange=(action (mut searchGameText))
    onSelectionChange=(action (mut selectedGame))
    search=searchGames as |gameResult|}}
    {{game-card-small class="game-autocomplete-item" game=gameResult}}
{{/paper-autocomplete}}
